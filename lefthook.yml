pre-commit:
  commands:
    format:
      run: pnpm format

pre-push:
  commands:
    lint-main:
      run: |
        branch=$(git rev-parse --abbrev-ref HEAD)
        if [ "$branch" = "main" ]; then
          pnpm lint
        else
          echo "Skipping lint on branch $branch"
        fi
