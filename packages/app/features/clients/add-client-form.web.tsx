export function AddClientForm() {
  return (
    <div className="card card-padded">
      <p className="text-sm text-[var(--color-secondaryText)]">Client creation for web lives in /clients/add via server actions.</p>
    </div>
  )
}

export type AddClientFormHandle = never
